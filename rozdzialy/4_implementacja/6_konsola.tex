\section{Konsola operatora}
Aplikacja, poza wykorzystaniem flysticków, mo¿e byæ sterowana za pomoc¹ specjalnej konsoli. Ka¿dorazowe skorzystanie z jaskini wymaga obecnoœci operatora, który uruchamia aplikacjê w trakcie prezentacji. Bêdzie on posiada³ dostêp do konsoli, a jego zadaniem bêdzie przenoszenie u¿ytkownika na konkretne wêz³y przy u¿yciu wyszukiwarki, a tak¿e w³¹czanie zaprogramowanych tras przegl¹dania grafu. Co wiêcej, konsola bêdzie posiada³a interfejs tworzenia tras umo¿liwiaj¹cy szybkie i wygodne tworzenie nowych plików. 

Interfejs konsoli operatora posiada dwie zak³adki - Search oraz Routes. Pierwsza jest odpowiedzialna za wyszukiwanie wêz³ów, na które ma zostaæ przeniesiony u¿ytkownik, druga s³u¿y w³¹czaniu tras. Obydwa elementy posiadaj¹ skrypt o nazwie Toggle, s³u¿¹cy odpowiedniemu prze³¹czaniu aktywnych elementów w konsoli. W obu zak³adkach znajduje siê obiekt przechowuj¹cy wyniki wyszukiwania, umieszczane w obiekcie zawieraj¹cym komponent Scroll Rect. Umo¿liwia on automatyczne utworzenie paska przewijania, co daje mo¿liwoœæ umieszczenia bardzo du¿ej iloœci elementów.  Dodatkowo w zak³adce Search znajduj¹ siê tak¿e obiekt o nazwie SearchInput odpowiedzialny za wpisywanie wyszukiwanego wêz³a, posiadaj¹cy skrypt InputField. Ponadto, nad konsol¹ znajduje siê tak¿e informacja o tym, jaki wêze³ jest aktualnie wybrany.

Wyszukiwanie wêz³ów odbywa siê na przygotowanym uprzednio pliku, zawieraj¹cym posortowane alfabetycznie nazwy artyku³ów i kategorii oraz ich ID. Przeszukiwanie listy odbywa siê za pomoc¹ algorytmu wyszukiwania binarnego. Lista elementów aktualizuje siê za ka¿dym wydarzeniem OnValueChanged obiektu SearchInput. Instancjonowane s¹ kolejne prefaby wyników wyszukiwania, w których zmieniana jest ich pozycja w pionie, nazwa i ikona symbolizuj¹ca artyku³ lub kategoriê. W momencie wybrania wêz³a wywo³ywane jest zdarzenie OnClick, do którego podpiêta jest funkcja HistoryController odpowiedzialna za wybranie wêz³a. T³o wybranego obiektu zmieniane jest na niebiesko.

Lista tras jest pobierana na podstawie aktualnie wybranej wersji Wikipedii. Prefab wyœwietlaj¹cy trasê posiada nazwê, d³ugoœæ trasy oraz przycisk s³u¿¹cy w³¹czaniu trasy. W celu utworzenia kolejnych elementów listy wykorzystywana jest funkcja Instantiate. W kolejnych instancjach zmieniana jest wysokoœæ obiektu, nazwa przycisku, nazwa trasy oraz d³ugoœæ trasy.  W momencie w³¹czenia trasy wywo³ywane jest zdarzenie OnClick(), do którego podpiêta jest funkcja z HistoryController wczytuj¹ca trasê o numerze indeksu, któr¹ posiada wciœniêty przycisk. Ponadto t³o wybranego obiektu zmieniane jest na niebieski.
